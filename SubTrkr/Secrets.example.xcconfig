// Copy this file to SubTrkr/Secrets.xcconfig and replace the placeholders.
// xcconfig treats // as a comment, so split the URL scheme here.
SUPABASE_URL = https:/$()/your-project-ref.supabase.co
SUPABASE_ANON_KEY = your-anon-key
